<!DOCTYPE html>
<html>
<head>
  <title><%= @title || "Recipes" %></title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <link href="/favicon.ico" rel="shortcut icon" type="image/x-icon" />

  <% if mobile_app? %>
    <% if current_user %>
      <%= yield :meta %>
    <% else %>
      <link rel="mobile-navigation" href="<%= new_session_url %>" title="Sign in" />
    <% end %>
  <% end %>


  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
  <link href="https://fonts.googleapis.com/css?family=Lato:300,400,700,900" rel="stylesheet">
  <%= stylesheet_link_tag "application", media: "all" %>

  <script src="https://code.jquery.com/jquery-3.2.1.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.11.0/umd/popper.min.js" integrity="sha384-b/U6ypiBEHpOf/4+1nzFpr53nxSS+GLCkfwBdFNTxtclqqenISfwAzpKaMNFNmj4" crossorigin="anonymous"></script>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <% if current_user %>
  <script>
    window.currentMenuPlan = new MenuPlan(<%= raw MenuPlanPresenter.new(current_user.current_menu_plan).to_json %>);
    window.currentMenuPlan._recipes = new Recipes(
      <%= raw ActiveModelSerializers::SerializableResource.new(current_user.current_menu_plan.recipes, adapter: :attributes).to_json %>
    );
  </script>
  <% end %>
</head>
<body>

  <%= render "layouts/navigation" unless mobile_app? %>

  <div class="container">
    <%= yield %>
  </div>

</body>
</html>
